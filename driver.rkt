#lang racket
(require lang/posn)
(require 2htdp/image)
(require 2htdp/universe)
;enemy tank 
(define-struct Etank (tank image)

  
  
 ) 

(define-struct bullet (pos image)) 

(define fire (E_tank player ) )

(define (player tank)
  (let* ((life 100))
   ((bullet 20))
   ((score 0 )) 

    (cond
      ;((if health is < 0 ) game over ) 
      ;((if fire is called) decrease ammo  )
      ;((if tank is hit) decrease life )
      ;((if confirmed hit) increase score)
      ; https://docs.racket-lang.org/teachpack/2htdpimage-guide.html
      )
    )
  )